# Pipecat voice AI framework with WebRTC + Telnyx + STT/TTS/LLM
pipecat-ai[silero,deepgram,openai,webrtc,telnyx]>=0.0.60

# Telnyx telephony
telnyx>=2.1.0

# Prebuilt WebRTC frontend UI (call button in browser)
pipecat-ai-small-webrtc-prebuilt>=0.0.4

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# HTTP client (for OpenClaw integration)
httpx>=0.28.0

# Configuration
python-dotenv>=1.0.0
